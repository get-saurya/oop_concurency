package org.example.multithreading;

public class Main {
    public static void main(String[] args) {
        System.out.println("Hey learners from Main ! Printed by : "+Thread.currentThread().getName());
       // Helper.doSomething();

        /* Print Hello world from a new thread
        * Create a task that you want to execute in a separate thread
        * Create a new thread
        * Assign the task to the thread
        * Start the thread */
    }
}
